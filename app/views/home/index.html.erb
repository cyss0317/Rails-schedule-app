<% modal_toggle = false %>


<%= render partial: "shared/modal", locals: { modal_toggle: modal_toggle } %>


<% if current_user %>
  <h1>Welcome, <%= current_user.full_name %></h1>
  <%= button_to "Sign Out", destroy_user_session_path, method: :delete, data: {confirm: "are you sure"} %>
  <div class="m-6 text-center">
    <%= month_calendar do |date| %>
      <%= date.day %>
    <% end %>
    <%= week_calendar do |date|  %>
      <%= date.day %>
    <% end %>
     <%#= render partial: "simple_calendar/calendar" %>
  </div>
<% else %>
  <%= link_to "Sign Up", new_user_registration_path %>
  <%= link_to "Sign In", new_user_session_path %>
<% end %>
<%# document.querySelector(".calendar-title").innerHTML.split(" ").join(",11,") %>
<script>
  document.addEventListener('DOMContentLoaded', () => {
    document.querySelectorAll(".day").forEach(day => {
      day.addEventListener("click", (e) => {
       console.log(document.querySelector(".calendar-title").innerHTML.split(" ").join(`,${e.target.innerText},`))
        document.querySelector("#myModal").style.display = "block"
      })
    })
  })
</script>